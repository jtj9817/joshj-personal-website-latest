---
import Icon from './Icon.astro';
const currentYear = new Date().getFullYear();
---

<footer>
	<p>Designed & Developed with <a href="https://astro.build/">Astro</a> &copy; {currentYear} Joshua Jadulco</p>
</footer>
<style>
	footer {
		display: flex;
		justify-content: center;
		align-items: center;
		margin-top: auto;
		padding: 1.5rem 1rem;
		text-align: center;
		color: var(--gray-400);
		font-size: var(--text-sm);
		background: transparent;
	}

	footer p {
		margin: 0;
	}

	footer a {
		color: var(--gray-400);
		text-decoration: none;
		transition: color var(--theme-transition);
	}

	footer a:hover {
		color: var(--accent-regular);
	}

	@media (min-width: 50em) {
		footer {
			padding: 1.5rem 2rem;
		}
	}
</style>
